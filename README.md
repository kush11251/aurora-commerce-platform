# Aurora Commerce Platform
A scalable e-commerce platform for managing products and orders
## Installation
1. Clone the repository: `git clone https://github.com/username/aurora-commerce-platform.git`
2. Navigate to the project directory: `cd aurora-commerce-platform`
3. Install dependencies: `npm install`
4. Run the application: `node src/index.js`